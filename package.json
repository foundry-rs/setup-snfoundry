{
  "name": "setup-snfoundry",
  "version": "1.0.0",
  "description": "Sets up Starknet Foundry in your Github Actions",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist/setup --source-map --license licenses.txt",
    "lint": "npm run fmt:check",
    "fmt:check": "prettier -c .",
    "fmt": "prettier -w ."
  },
  "keywords": [],
  "author": "Foundry",
  "license": "MIT",
  "dependencies": {
    "@actions/cache": "^3.2.2",
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/glob": "^0.4.0",
    "@actions/http-client": "^2.1.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "prettier": "^3.0.3"
  }
}
